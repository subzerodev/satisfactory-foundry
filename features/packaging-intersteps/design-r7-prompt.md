# #113 packaging intersteps design review r7

Review the one-shot simplify folds in
`features/packaging-intersteps/brainstorm-spec.md` under
`/home/subzerodev/workspace/satisfactory-foundry/.worktrees/s22-113-intersteps`.

The persisted pair key is now packageRecipeId only; discovery derives the unique
reverse recipe. One pure, recomputable deriveLinkPlan boundary owns projection
failure and both transport results without introducing cached state. Permanent
saturation warning prose was removed in favor of computed route results; seed
and separate-path advisories remain. Verify the full design and return exact
citations plus one contract verdict.
