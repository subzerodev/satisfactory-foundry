# Operating model — this repo

This repo follows the agent-led-development plugin's operating model (board + wiki
as the audited source of truth). The canonical long-form lives in the plugin's
operating-model reference doc; this stub records only this repo's specifics.

- **Board:** project `#21` — http://10.0.0.69:3000/sudohworks/satisfactory-foundry/projects/21
- **Columns:** `Backlog → To Do → In Progress → Done` (no Review/Blocked columns:
  review verdicts are audit comments, `blocked` is a label)
- **Marker:** `.forgejo-ops.toml` (board id, columns, labels, stages)

This file is the **source**; the "Operating Model" wiki page is a mirror of it
(canonical-source rule). Edit here, then mirror outward — never the reverse.
